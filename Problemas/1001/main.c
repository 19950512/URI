#include <stdio.h>

int main(){

    int a;
    int b;
    int x;

    // reads and stores input
    scanf("%d", &a);
    scanf("%d", &b);

    x = a + b;

    // displays output
    printf("X = %d\n", x);
    return 0;
}
